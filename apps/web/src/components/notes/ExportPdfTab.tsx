import MaintenancePlaceholder from "./MaintenancePlaceholder";

/**
 * Onglet "Export PDF".
 * Permettra à terme l'export en PDF des bulletins, relevés de notes,
 * classements et comptes-rendus de délibération.
 *
 * @status EN MAINTENANCE
 */
export default function ExportPdfTab() {
  return (
    <MaintenancePlaceholder
      titre="Export PDF"
      description="Ce module permettra l'export en PDF des bulletins scolaires, relevés de notes, classements et comptes-rendus de délibération, avec mise en page personnalisable."
    />
  );
}
